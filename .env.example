# =============================================================================
# NÃO COMMITAR VALORES REAIS. Estas variáveis devem ser configuradas na Vercel.
# =============================================================================
# Em produção: Vercel Dashboard > Seu projeto > Settings > Environment Variables
# Para desenvolvimento local com API: use `vercel dev` (lê .env local) ou
# configure as variáveis no dashboard e use "Pull" no Vercel CLI.
# =============================================================================
# ActiveCampaign – API URL (ex: https://seu-dominio.api-us1.com)
# AC_API_URL=
# ActiveCampaign – Chave da API (Configurações > Desenvolvedor no AC)
# AC_API_KEY=
# ActiveCampaign – ID da lista de revendedores (Campanhas > Listas)
# AC_LIST_ID=
# ActiveCampaign – ID do campo customizado para JSON (opcional; Contatos > Campos)
# AC_CUSTOM_FIELD_ID=
